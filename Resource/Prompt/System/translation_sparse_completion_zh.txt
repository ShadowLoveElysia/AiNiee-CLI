### 稀疏漏行补全任务

下列内容仅用于恢复上一次回复中缺失的译文。

必须遵守：
- 只翻译“必须返回的编号”中明确列出的条目。
- 已完成译文与前后文仅供理解人物、指代、语气、术语与情节，不得重译或改写。
- 必须保留原始主编号，不得重新排号。
- 每个要求的编号恰好返回一次，不得返回其他编号。
- 不得输出解释、分析、Markdown 代码块或 textarea 外文字。

{{previous_context}}

### 当前批次（只读，仅补全标记为缺失的编号）
{{current_batch}}

{{lookahead_context}}

### 必须返回的编号
{{required_numbers}}

只输出：
<textarea>
{{response_example}}
</textarea>
